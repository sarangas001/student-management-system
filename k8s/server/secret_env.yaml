apiVersion: v1
kind: Secret
metadata:
  name: env-secrets
type: Opaque
stringData:
  VITE_BACKEND_URL: ${VITE_BACKEND_URL}
  NODE_ENV: ${NODE_ENV}
  JWT_SECRET: ${JWT_SECRET}
  MONGO_URI: ${MONGO_URI}